version: "3"
services:
  single:
    build:
      context: .
      dockerfile: Dockerfile.single
  multiple:
    build:
      context: .
      dockerfile: Dockerfile.multiple
    labels:
      expose.command.multiple.test: testcommand
  test:
    image: composer:latest
    labels:
      expose.command.single: composer
