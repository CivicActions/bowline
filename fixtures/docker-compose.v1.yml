single:
  build: Dockerfile.single
singleimg:
  image: alpine:latest
  labels:
    exposed.command.single: testcommand
multiple:
  build: Dockerfile.multiple
  labels:
    exposed.command.multiple.test: testcommand
test:
  image: alpine:latest
